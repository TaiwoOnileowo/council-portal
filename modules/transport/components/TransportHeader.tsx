import PageHeader from "@/components/ui/PageHeader";

export default function TransportHeader() {
  return (
    <PageHeader
      title="Transport"
      subtitle="Book rides with council-approved transport vendors"
    />
  );
}
